#pragma once
#include <string>
#include "tiq.h"

namespace global {
  inline Game game;
  inline std::string menuVersion = "0.0.1";
  inline std::string menuTitle = "Ananke";

  inline std::vector<GameVersion> flashVersions;
}
